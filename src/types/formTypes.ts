export type FormState = {
    username: string;
    email: string;
    password: string;
}

export type FormValidation = {
    username: string | null;
    email: string | null;
    password: string | null;
}
